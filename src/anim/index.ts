/**
 * `@/anim` barrel — tweening and easing.
 *
 * Provides {@link Tween} (sequenced property interpolation), {@link TweenManager}
 * (an {@link EngineModule} that advances active tweens), and the {@link Ease}
 * library of standard easing curves.
 */
export { Tween } from './Tween';
export { TweenManager } from './TweenManager';
export { Ease } from './Ease';
export type { Easing } from './Ease';
